import 'package:flutter/material.dart';

class SportsEventPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crear evento deportivo'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de creación de evento deportivo'),
      ),
    );
  }
}